int main() {
    // Combined declaration and assignment (should be split)
    int x = 10;
    int y = 20;
    double z = 3.14;
    
    // Some operations
    x = x + y;
    y = x * 2;
    
    return 0;
}
